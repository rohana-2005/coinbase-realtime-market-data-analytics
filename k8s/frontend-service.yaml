apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: coinbase
spec:
  selector:
    app: frontend
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30090  # Access frontend at Minikube-IP:30090
      name: http
